	* .webp image format support
	* fix: shell scripts placeholders
	* fix: creating entity from selection now deletes source entity if it becomes empty container
	* make rotate dialog non-modal
	* filter: include func_static in world filter (so it is not hidden by filter entities)
	* texbro: improve text legibility (shadow/outline)
	* misc_model: support model2 key
	* light: draw light style number
	* group entities: force arrow drawing for func_door and func_button
	* Select Touching Tall command (selects touching in 2D, ignoring height)
	* option to disable dots on selected faces when not in face mode
